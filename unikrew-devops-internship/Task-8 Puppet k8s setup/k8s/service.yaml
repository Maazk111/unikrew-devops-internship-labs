apiVersion: v1
kind: Service
metadata:
  name: puppet-frontend-service
spec:
  type: NodePort
  selector:
    app: puppet-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
